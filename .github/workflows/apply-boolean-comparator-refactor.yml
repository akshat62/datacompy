name: Apply Boolean Comparator Refactor

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  refactor:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/boolean-comparator-parity
          fetch-depth: 0

      - name: Register dedicated Boolean comparators
        shell: python
        run: |
          from pathlib import Path

          Path("datacompy/comparator/__init__.py").write_text('''#
          # Copyright 2026 Capital One Services, LLC
          #
          # Licensed under the Apache License, Version 2.0 (the "License");
          # you may not use this file except in compliance with the License.
          # You may obtain a copy of the License at
          #
          #     http://www.apache.org/licenses/LICENSE-2.0
          #
          # Unless required by applicable law or agreed to in writing, software
          # distributed under the License is distributed on an "AS IS" BASIS,
          # WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
          # See the License for the specific language governing permissions and
          # limitations under the License.

          """Comparator classes."""

          from datacompy.comparator.array import (
              PandasArrayLikeComparator,
              PolarsArrayLikeComparator,
              SnowflakeArrayLikeComparator,
              SparkArrayLikeComparator,
          )
          from datacompy.comparator.boolean import (
              PandasBooleanComparator,
              PolarsBooleanComparator,
              SnowflakeBooleanComparator,
              SparkBooleanComparator,
          )
          from datacompy.comparator.numeric import (
              PandasNumericComparator,
              PolarsNumericComparator,
              SnowflakeNumericComparator,
              SparkNumericComparator,
          )
          from datacompy.comparator.string import (
              PandasStringComparator,
              PolarsStringComparator,
              SnowflakeStringComparator,
              SparkStringComparator,
          )

          __all__ = [
              "PandasArrayLikeComparator",
              "PandasBooleanComparator",
              "PandasNumericComparator",
              "PandasStringComparator",
              "PolarsArrayLikeComparator",
              "PolarsBooleanComparator",
              "PolarsNumericComparator",
              "PolarsStringComparator",
              "SnowflakeArrayLikeComparator",
              "SnowflakeBooleanComparator",
              "SnowflakeNumericComparator",
              "SnowflakeStringComparator",
              "SparkArrayLikeComparator",
              "SparkBooleanComparator",
              "SparkNumericComparator",
              "SparkStringComparator",
          ]
          '''.replace("          ", ""))

          def register(path: str, class_name: str) -> None:
              file_path = Path(path)
              text = file_path.read_text()
              array_name = class_name.replace("Boolean", "ArrayLike")
              numeric_name = class_name.replace("Boolean", "Numeric")

              import_anchor = f"    {array_name},\n"
              import_line = f"    {class_name},\n"
              if import_line not in text:
                  if import_anchor not in text:
                      raise RuntimeError(f"Import anchor not found in {path}")
                  text = text.replace(import_anchor, import_anchor + import_line, 1)

              list_anchor = f"    {array_name}(),\n"
              list_line = f"    {class_name}(),\n"
              if list_line not in text:
                  if list_anchor not in text:
                      raise RuntimeError(f"Comparator-list anchor not found in {path}")
                  text = text.replace(list_anchor, list_anchor + list_line, 1)

              file_path.write_text(text)

          register("datacompy/polars.py", "PolarsBooleanComparator")
          register("datacompy/spark.py", "SparkBooleanComparator")
          register("datacompy/snowflake.py", "SnowflakeBooleanComparator")

      - name: Commit corrected design
        run: |
          rm .github/workflows/apply-boolean-comparator-refactor.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add datacompy/comparator/__init__.py datacompy/polars.py datacompy/spark.py datacompy/snowflake.py .github/workflows/apply-boolean-comparator-refactor.yml
          git commit -m "Register dedicated Boolean comparators by backend"
          git push origin HEAD:feat/boolean-comparator-parity
